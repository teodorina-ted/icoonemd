const BASE = "/images";

const PhotoBanner = () => (
  <div className="w-full overflow-hidden max-h-[420px]">
    <img src={`${BASE}/img5.png`} alt="ICOONE процедура" className="w-full h-full object-cover block" />
  </div>
);

export default PhotoBanner;
